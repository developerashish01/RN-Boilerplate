// __ Home

export const HOME_PAGE_URL = "home-new/";

export const GET_DONOW_ALBUM = (slug: string) => {
  return `do_now/album/${slug}/`;
};

export const GET_EMPLOYEES = "http://dummy.restapiexample.com/api/v1/employees";
