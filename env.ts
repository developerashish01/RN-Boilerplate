import Config from "react-native-config";

/**
 * Encapsulate the access of environment variables that can be allowed for react native
 */

export default {
  API_URL: Config.API_URL,
  TARGET_NAME: Config.TARGET_NAME,
};
